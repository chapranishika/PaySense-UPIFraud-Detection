# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-multipart==0.0.31  # bumped 2026-08-26 audit -- pip-audit found real CVEs in 0.0.9

# ML
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
xgboost==2.1.1
imbalanced-learn==0.12.3
shap==0.45.1
joblib==1.4.2
scipy==1.13.1
matplotlib==3.9.0

# Security (Phase 1 — JWT + rate limiting)
# python-jose, python-dotenv, cryptography versions bumped 2026-08-26 audit:
# pip-audit found real CVEs in the previously-pinned versions. All bumps
# verified against the full test suite (214/214 passing) before pinning.
# cryptography is python-jose's own dependency, bumped for the same reason.
# 2026-08-28: python-jose bumped 3.4.0->3.5.0 specifically because it relaxes
# its own pyasn1 pin from "<0.5.0" to ">=0.5.0" -- that pin was blocking a fix
# for 6 known pyasn1 CVEs (PYSEC-2026-2263/3455/3456/3457). pyasn1 pinned to
# the fixed 0.6.4 in the same pass. Verified against the full test suite
# (230/230 passing) before pinning.
python-jose[cryptography]==3.5.0
pyasn1==0.6.4
slowapi==0.1.9
python-dotenv==1.2.2
passlib[bcrypt]==1.7.4
httpx==0.27.0          # Gemini API calls + test client
cryptography==50.0.0
